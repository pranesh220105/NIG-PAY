Place branding assets here before generating icon/splash:

- `app_icon.png` (1024x1024)
- `splash_logo.png` (at least 512x512, transparent background preferred)

Then run:

```bash
flutter pub run flutter_launcher_icons
flutter pub run flutter_native_splash:create
```
